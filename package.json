{
  "name": "portfolio-backend",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "babel server --out-dir build/server && babel data --out-dir build/data",
    "start": "npm run build && node ./build/server/app.js"
  },
  "dependencies": {
    "body-parser": "~1.18.2",
    "debug": "~2.6.9",
    "express": "~4.15.5",
    "morgan": "~1.9.0"
  },
  "devDependencies": {
    "babel-core": "^6.26.3",
    "babel-loader": "^7.1.4",
    "babel-preset-es2015": "^6.24.1"
  }
}
