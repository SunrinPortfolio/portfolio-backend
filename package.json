{
  "name": "portfolio-backend",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "babel src --out-dir build",
    "start": "node ./build/server/app.js",
    "start-with-build": "npm run build && npm run start"
  },
  "dependencies": {
    "@babel/polyfill": "^7.0.0",
    "body-parser": "~1.18.2",
    "debug": "~2.6.9",
    "express": "~4.15.5",
    "morgan": "~1.9.0"
  },
  "devDependencies": {
    "@babel/cli": "^7.0.0",
    "@babel/core": "^7.0.0",
    "@babel/preset-env": "^7.0.0",
    "babel-loader": "^8.0.0"
  }
}
